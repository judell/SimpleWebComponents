create table cities (
    id integer primary key,
    city text,
    state text,
    population integer
);


create table books (
    id integer primary key,
    title text,
    author text,
    year integer
);

